<% @links.each do |link| %>
    <div class=link-container>
      <%= link_to link.url, link_path(link), class: 'link' %>
      <%= link_to '(Delete)', link_path(link), method: :delete %>
    </div>
<% end %>
